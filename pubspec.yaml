name: screen_brightness
description: Flutter Screen Brightness.
version: 0.0.1
author: sakura-sakai
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.sakura.screen_brightness
        pluginClass: ScreenBrightnessPlugin
      ios:
        pluginClass: ScreenBrightnessPlugin

